global:
  message: Welcome to microservices environment
logging:
  level:
    root: INFO
management:
  endpoints:
    web:
      exposure:
        includes: health, info, refresh
endpoint:
  health:
    show-details: always
